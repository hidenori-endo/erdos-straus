# Factor-rich injection

- status: rejected as a standalone route
- 由来: 元会話 Turn 030

Injecting many known factors through a highly composite L creates a narrow k range, but the target -1 is usually absent from the resulting roster. The local miss rate is structural, so this construction does not yield a global proof.

